import {Spinner} from './Spinner.styles';

export default Spinner;

//the spinner does not need any more html formatting nor does it need information from the api, so we will just straight up export this as it is and import it into our homepage